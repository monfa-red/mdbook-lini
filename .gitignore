target/
.DS_Store

# Local-only path override for developing against ../lini
.cargo/

# `mdbook build tests/book` by hand lands here; `tests/book.rs` builds to
# `target/` instead, so the suite leaves the tree alone.
tests/book/out/
